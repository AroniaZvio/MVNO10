{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "main": "dist/server.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "dev:local": "ts-node-dev --respawn --transpile-only src/server.ts",
    "dev:production": "ts-node-dev --respawn --transpile-only src/server.ts",
    "dev:render": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "build:production": "tsc",
    "build:render": "tsc",
    "start": "node dist/server.js",
    "start:production": "node dist/server.js",
    "start:render": "node dist/server.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:migrate:deploy": "prisma migrate deploy",
    "prisma:studio": "prisma studio",
    "prisma:studio:local": "prisma studio",
    "prisma:studio:production": "prisma studio",
    "seed": "ts-node-dev src/seed.ts",
    "seed:admin": "ts-node-dev src/scripts/seedAdmin.ts",
    "db:reset": "prisma migrate reset --force",
    "db:setup": "prisma migrate dev && npm run seed"
  },
  "engines": {
    "node": "20.x"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^7.0.5",
    "zod": "^4.0.17"
  },
  "devDependencies": {
    "@prisma/client": "^6.15.0",
    "@types/bcrypt": "^6.0.0",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^24.2.1",
    "@types/nodemailer": "^7.0.1",
    "prisma": "^6.15.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.2"
  }
}
